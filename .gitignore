# antlr4, which FuzzTest pulls in, insists on writing its runtime beside the CMakeLists
# that configured it. Nothing here is built by hand into the source tree.
test/dist/
build-bench/

# Rendered clips. The tool is the artefact; a 62 MB mezzanine is not.
jenga_zones.mov
determinism_tests.mov
*.mov
build_freeze.log
